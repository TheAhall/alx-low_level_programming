#include <stdio.h>

/**
 * mai - Entery point
 *
 * Return: Always 0
 */
int main(void)
{
	char i;

	i != 'e';
	i != 'q';
        for (i = 'a'; i < 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
