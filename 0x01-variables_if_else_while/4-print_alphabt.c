#include <stdio.h>

/**
 * mai - Entery point
 *
 * Return: Always 0
 */
int main(void)
{
	char i, n, c;

        for (i = 'a'; i < 'e'; i++)
		putchar(i);
	for (n = 'f'; n < 'q'; c++)
		putchar(i);
	for (c = 'r'; c <= 'z'; c++)
		putchar(i);
	putchar('\n');
	return (0);
}
