#include "main.h"
#include <string.h>

/**
 * _strlen - function for showing the length
 * @s: variable to know its length
 * Return: 0
 */
int _strlen(char *s)
{
	size_t i = strlen(s);
	return (i);
}
